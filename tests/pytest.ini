[pytest]
filterwarnings =
# turn all warnings into error
# error
    ignore::UserWarning
# ignore
#    ignore::FutureWarning

# https://docs.pytest.org/en/latest/logging.html
log_format = %(levelname)s
    %(message)s
# higher than debug
log_level = 20